# NOT-TO-DO

Fehlerprotokoll fuer dieses Projekt. Eintraege werden bei Session-Start gelesen.
Format: `| Datum | Tag | Fehler | Vermeidung |`

| Datum | Tag | Fehler | Vermeidung |
|-------|-----|--------|------------|
| 2026-06-11 | python, klipper, buffer | AUTO+forward Pending-Streams werden in _tick_pending_chunk vom Demand-Modulator (_compute_target_feed_speed) gespeist — nach Druckende ist Demand 0, fester Distanz-Stream (Park-Fill) starb nach 1 Sub-Chunk bei bereits gesetztem One-Shot-Latch (Codex-Repro: pending 141->0) | Feste AUTO-Forward-Streams brauchen Bypass-Flag (_park_full_active-Muster); HALL2/HALL1-Aborts MUESSEN vor dem Bypass bleiben; Flag auf JEDEM Stream-Ende-Pfad raeumen (_abort_signalled, hall_full, Exhaustion, _halt_motion, _submit_move-Session-Reset) und nur armen wenn pending > 0 |
| 2026-06-11 | codex, powershell | codex exec mit langem Inline-Prompt als Positional-Arg hing 90 min ohne Arbeit (0.11s CPU, leerer Output — gepuffertes Select-Object verschleiert den Hang) | Prompt via stdin pipen: `$prompt \| codex exec --sandbox read-only --skip-git-repo-check -`; Hang-Diagnose: Get-Process CPU ~0 nach Minuten = blockierender Prompt, kill + retry statt warten |
| 2026-05-13 | python, klipper, stepcompress | t0-Clamping (`t0 = max(forced_t0, lme, en, mcu_now)`) schuetzt NICHT gegen `stepcompress.last_step_clock` — Klipper exposed das nicht in Python. P7-76 hatte den Bug: legitimer Anchor rueckte last_step_clock auf 551.18s vor, nachfolgender geclampter Submit bei 551.13s erzeugte negative interval c=1 i=-500471 | Bei `forced_t0=None` UND `th_time > mcu_now + MAX_T0_LOOKAHEAD` immer SKIPEN statt clampen (P7-77 B). Plus Watchdog HARD-blocken bei `print_stats.state=='printing'` (P7-77 A) — im aktiven Print pflegt `_on_mcu_flush` + P7-73 den Cursor race-frei. |
| 2026-05-13 | klipper, eifel-logs | Eifel-Joes klippy.log kann MEHRERE Sessions mit unterschiedlichen `buffer_feeder.py`-Versionen enthalten (Branch master, file untracked = manuell kopiert). Diagnose-Subagenten kamen zu widersprueglichen Schluessen weil sie verschiedene Sessions sahen | Vor Diagnose IMMER session-grenzen mit `grep "Starting Klippy"` finden + Version-Marker pro Session pruefen (P7-67 boot-marker, P7-75 auto-anchor-fired, P7-73 forced_t0 clamped). Plus Issue-Kommentar an Eifel mit Bitte um `md5sum klippy/extras/buffer_feeder.py` fuer eindeutige Version-ID. |
| 2026-05-12 | hardware, klipper, buffer | flush_callback_chunk_mm >= 40 grindet Filament in den Buffer-Eingangs-Zahnraedern (Mellow LLL Plus). AUTO-Bang-Bang submittet einen kompletten Chunk und kann ihn NICHT bei HALL2/HALL1-Trigger abbrechen → Overshoot um bis zu eine Chunk-Laenge → Filament wird gegen vollen Puffer gepresst | P7-66b loest dies via Move-Splitting: interrupt_chunk_mm (Default 9.0) capt jedes submittierte Trapezoid. _tick_pending_chunk prueft HALL2/HALL1 zwischen Sub-Chunks → Worst-Case-Overshoot = interrupt_chunk_mm. Option I (Endstop-Pattern via home_start) waere natives Klipper-API, ist aber inkompatibel mit Streaming-Trapq (drip_move + flush_step_generation blocken). |
| 2026-05-12 | python, klipper, fakes | FakePrinter ruft klippy:connect-Handler nie auf — _handle_connect (das _stepper_enable verkabelt) blieb stumm. Tests prueften nichts gegen den realen Enable-Pfad (Codex-Finding P7-66 R6) | FakePrinter.fire_event(event, ...) helper hinzufuegen, in make_feeder()-Helpern `printer.fire_event('klippy:connect')` aufrufen. Pre-existing tests koennen aber auch ohne diesen Helper laufen — nur die Tests, die explizit Stepper-Enable-Verhalten exercisen, MUESSEN ihn rufen. |
| 2026-04-24 | python, klipper, stepcompress | Detach/Reattach via set_trapq(None) reicht nicht um stepcompress.last_step_clock zu re-anchorn fuer never-stepped Stepper — vier Iterationen P7-2..P7-5 als Sackgasse | Boot-Anchor-Step (P7-18) statt Detach-Architektur. set_position triggert NICHT stepcompress_set_last_position fuer detachten Stepper. Mainline-API gibt es nicht — Workaround mit echtem Step beim Boot. |
| 2026-04-25 | klipper, macros, jinja | Jinja2-Macros mit `{% if %}`-Branches haben kein try/finally bei Klipper-error/M112 — Sync-State kann haengen bleiben | Sync/Cleanup-Logik mit guaranteed-Cleanup gehoert in Python (cmd_*) mit Python-try/finally, nicht in Macro. P7-28 BUFFER_UNLOAD_FILAMENT als Beispiel. |
| 2026-04-26 | python, refactoring | "no behavior change" Commit-Body bei 134-Zeilen-Refactor mit nur 1 Smoke-Test als Coverage = Wunschdenken | Vor groesserem Refactor Charakterisierungs-Tests fuer die kritischen Pfade schreiben. Mindestens HALL1-Bypass-Branches und Recovery-Flag-Reset-Sites characterizen. |
| 2026-04-26 | python, codex | Codex schreibt Commits direkt aber `.git/index.lock` permission deny moeglich — uncommitted Aenderungen im Worktree | Wenn Codex commit failed: Worktree-Aenderungen pruefen + selbst committen. Codex-Output liest "Du wirst benachrichtigt" auch bei Hang — git status verifizieren statt blind warten. |
| 2026-04-26 | python, fakes | FakeConfig.getboolean(`bool("0")`) ist truthy in Python (`bool("0")=True`) — Test-Mock weicht von Klipper-Realitaet ab | Klipper-konformer FakeConfig.getboolean: explizit "0"/"false"/"no" → False, "1"/"true"/"yes" → True, ValueError fuer alles andere. |
| 2026-04-26 | jinja, klipper | Macro-Dispatcher (`{% if %} CMD_A {% else %} CMD_B {% endif %}`) leitet User-Args nicht automatisch weiter | `{rawparams}` an jeden Branch anhaengen damit `UNLOAD_FILAMENT TIP_CYCLES=2` durchgereicht wird. Sonst wirken Macro-Variables nach Flag-Switch nicht mehr. |
| 2026-04-26 | python, cleanup | Cleanup-Guard-Flag NACH den state-Mutationen setzen → Mid-Mutation-Exception laesst halb-mutated state ohne armed cleanup zurueck (Codex MEDIUM in P7-36) | Arming-Flag VOR den Mutationen setzen + try/except mit best-effort rollback im except-Branch. Flag erst nach erfolgreichem rollback clearen, damit recursive Failures kein Doppel-Rollback triggern. |
| 2026-04-26 | python, klipper | Multi-Instanz `load_config_prefix`-Module mit `register_command` (statt `register_mux_command`) crashen beim Boot der zweiten Instanz mit "command already registered" (Reviewer #4 P7-36) | Bei `load_config_prefix=True` mit klar instanz-spezifischer Config (z.B. EXTRUDER=) immer `register_mux_command(name, mux_key, instance_value, handler)` verwenden. Pattern: kinematics/extruder.py:ExtruderStepper. |
| 2026-04-26 | python, klipper, mux | Nach mux-Migration alle internen `gcode.run_script("BUFFER_FOO ...")` ohne `BUFFER=<name>` aufrufen brechen den Workflow (Codex HIGH P7-41). FakeGCode-Mock fängt den Bug nicht weil er kein echtes mux-dispatching macht | Nach `register_mux_command` jeden internen nested call (`BUFFER_*`, `EXTRUDER_*` etc.) auch mit Mux-Selector versehen. `grep "run_script.*BUFFER_"` als pre-commit-check. Test: nested calls als Mock-Roundtrip via FakeGCode.commands lookup verifizieren, nicht nur raw-string-assert. |
| 2026-04-26 | jinja, klipper, macros | LOAD_FILAMENT-Macro abortet nach Phase 3 OVERFLOW_OK=1 weil `BUFFER_AUTO_ON` bei aktivem HALL1 raised → der finale `G1 E{slow_dist}` Push durch die Düse läuft NIE (Hardware-Test 2026-04-26). Erst durch Operator-Bericht entdeckt — Tests haben den Macro-Pfad nicht abgedeckt | Vor jedem `BUFFER_AUTO_ON` im Macro die HALL1-Realität prüfen: `{% elif bf.hall_overflow %} M118 skip {% else %} BUFFER_AUTO_ON {% endif %}`. Internes `cmd_BUFFER_AUTO_ON` raised hart bei HALL1 — die internen Auto-Engage-Hooks im Code (auto_engage_on_boot/print_start) machen es richtig, das Macro nicht. Pattern: bei jedem cmd_*-Aufruf der HARD-RAISES kennt, im Macro defensiv vorprüfen. |
| 2026-04-26 | klipper, hardware, sync | LOAD-Phase 3/3 (G1 E{slow_dist}) mit `state=AUTO` + Bang-Bang-Hysterese erlaubt Filament-Durchziehen durch den Buffer: bei nur 30mm Buffer-Arm-Weg fällt der Arm durch HALL1→HALL3 BEVOR Bang-Bang reagiert (Reaktionszeit > Arm-Fallzeit). Hardware bricht weil "durchziehen funktioniert nicht" — Stepper muss von Sekunde 1 mit-fördern (Operator-Hardware-Test 2026-04-26) | LOAD Phase 3/3 nutzt `BUFFER_SYNC_TO_EXTRUDER` analog zum UNLOAD-Tip-Forming-Pattern. SYNC bindet Buffer-Stepper 1:1 an Extruder-Trapq → kein Sliding möglich, mechanisch garantierte Mitarbeit. Symmetrie zwischen LOAD und UNLOAD. Bang-bang ist hysterese-driven und damit nicht ausreichend für engsensorierte Buffer (<50mm Arm-Weg). |
